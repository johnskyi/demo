server:
  port: 8086
spring:
  liquibase:
    enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: test
    password: test
  h2:
    console:
      enabled: true

  jpa:
    show-sql: true

